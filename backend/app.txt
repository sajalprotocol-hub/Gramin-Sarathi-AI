from flask import Flask, jsonify

# Create a new web application
app = Flask(__name__)

# Create a specific "address" or "endpoint" for our app called /ask
@app.route("/ask")
def process_question():
    # When someone visits this address, send back a simple message
    response_data = {"message": "Hello! The backend server is working!"}
    return jsonify(response_data)

# This part is needed to make the "Run" button work in some editors
if __name__ == "__main__":
    app.run(debug=True)
